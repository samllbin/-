/**
 * @param {string} s
 * @return {boolean}
 */
var isValid = function (s) {
  let str = Array.from(s);
  let stack = [];
  if (str.length % 2 !== 0) return false;
  for(let i = 0;i < str.length;i++) {
};
